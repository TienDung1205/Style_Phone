mixin sub-menu(items, path)
    ul
        each item in items
            li
                a(href=`${path}/${item.slug}`) #{item.title}
                    if item.child && item.child.length > 0
                        +sub-menu(item.child, path)